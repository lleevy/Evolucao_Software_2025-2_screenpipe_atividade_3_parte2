# ==============================================================================
# MELHORIA 2: Linter de Ortografia (Categoria: Linter/Formatação)
# ------------------------------------------------------------------------------
# MOTIVAÇÃO E BENEFÍCIOS TÉCNICOS:
#
# 1. Estratégia "Fail Fast" (Falhar Rápido):
#    O pipeline principal de Rust deste projeto demora entre 20 a 40 minutos para
#    dar feedback. Este workflow roda em paralelo e completa a verificação em
#    menos de 1 minuto. Se houver um erro simples de digitação, o desenvolvedor
#    é avisado instantaneamente, economizando tempo de espera e recursos de
#    computação do GitHub Actions.
#
# 2. Qualidade de Código e Legibilidade:
#    Typos em nomes de variáveis, funções ou comentários dificultam a manutenção
#    e a busca no código (grep). Este linter garante que "function" não vire
#    "funtion" e que a documentação esteja profissional.
#
# 3. Solução Agnóstica (Backend + Frontend):
#    Ao contrário do 'cargo fmt' (só Rust) ou 'eslint' (só JS/TS), o crate-ci/typos
#    verifica todo o repositório de uma vez. Ele cobre:
#    - Código Rust (.rs)
#    - Código TypeScript/React (.ts, .tsx)
#    - Documentação (.md)
#    - Configurações (.json, .toml, .yaml)
#
# 4. Baixa Manutenção:
#    A ferramenta possui baixíssima taxa de falsos positivos e não requer
#    instalação complexa de dependências (como npm install ou cargo build),
#    o que a torna extremamente leve e rápida de executar.
# ==============================================================================


name: Spellcheck (Fast Feedback)

# GATILHOS: Quando esse robô deve acordar?
on:
  push:
    branches: ["main"] # Protege a branch principal
  pull_request:        # Roda em qualquer PR aberto (para barrar erros antes do merge)
  workflow_dispatch:   # Permite rodar manualmente clicando num botão (útil para testes)

jobs:
  typos:
    name: Verificação Ortográfica
    runs-on: ubuntu-latest # Usa Linux (inicia em segundos)
    
    steps:
      # 1. Baixa o código do repositório para a máquina virtual
      - name: 📥 Checkout do código
        uses: actions/checkout@v4

      # 2. Roda o verificador de Typos
      # Ele lê Rust, TypeScript, HTML, CSS e Markdown automaticamente.
      - name: 🔍 Rodar Linter de Ortografia
        uses: crate-ci/typos@master
        with:
          # files: . (Opcional, por padrão ele já lê tudo)
          # Se quiser ignorar arquivos específicos, criamos um arquivo de config depois,
          # mas para esta atividade, o padrão funciona bem.
          ignore_hidden: false
